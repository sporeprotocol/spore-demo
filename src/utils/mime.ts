import { IMAGE_MIME_TYPE } from '@mantine/dropzone';

export { IMAGE_MIME_TYPE };
export const TXT_MIME_TYPE = ['text/markdown', 'text/plain'];

export const SUPPORTED_MIME_TYPE = [...IMAGE_MIME_TYPE];
